# Learnings

- [$env/dynamic/private doesn't resolve in vitest — split the pure logic into its own module](./env-dynamic-private-doesn-t-resolve-in-vitest-split-the-pure.md) - Vitest can't resolve `$env/dynamic/private` — importing it in a module under test fails at import time.
